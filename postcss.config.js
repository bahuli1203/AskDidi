// Intentionally empty.
// Tailwind is handled by the @tailwindcss/vite plugin in vite.config.js,
// so no PostCSS plugins are needed here. This file exists to override any
// stray postcss config in parent directories (e.g. C:\postcss.config.mjs).
export default {
    plugins: {},
};
